node_modules/

# build output
build/
.wrangler/
.react-router/

# local env — never commit secrets
.dev.vars
.env
.env.*

# working notes and credential env files for the Supabase Management API and
# wrangler. Nothing here has ever been committed and nothing here ever should
# be: the session stages with `git add -A`, and this is the only thing
# standing between that and a token in the history.
scratchpad/

# supabase local
supabase/.temp/
supabase/.branches/

# generated types
worker-configuration.d.ts

# os / editor
.DS_Store
*.log
